<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.highway.tunnelMonitoring.mapper.power.EldMapper">
    <select id="eldFindAll" resultType="com.highway.tunnelMonitoring.domain.power.eld.Eld">
        SELECT * FROM (
                          SELECT a.*, ROWNUM rnum
                          FROM (
                                   SELECT * FROM Eld
                                   WHERE LINK_ID = #{link_id}
                                   ORDER BY
                                        <if test="sort_column == 'eld_id'">
                                            TO_NUMBER(REGEXP_SUBSTR(eld_id, '\d+'))
                                        </if>
                                        <if test="sort_column == 'instl_lc'">
                                            instl_lc
                                        </if>
                                        <if test="sort_column == 'instl_de'">
                                            instl_de
                                        </if>
                                        <if test="sort_column == 'model_nm'">
                                            model_nm
                                        </if>
                                        <if test="sort_direction == 'asc'">
                                            ASC
                                        </if>
                                        <if test="sort_direction == 'desc'">
                                            DESC
                                        </if>
                               ) a
                      )
        WHERE rnum BETWEEN #{offset} + 1 AND #{offset} + #{limit}
    </select>

    <select id="eldCountAll" resultType="int">
        SELECT COUNT(*) FROM Eld WHERE LINK_ID = #{link_id}
    </select>

    <select id="eldMonitorCountAll" resultType="int">
        SELECT COUNT(*) FROM ELD_STTUS WHERE LINK_ID = #{link_id}
    </select>

    <select id="eldAlarmCountAll" resultType="int">
        SELECT COUNT(*) FROM ELD_ALARM_HISTORY WHERE LINK_ID = #{link_id}
    </select>


    <insert id="eldEnroll">
        insert into Eld(ELD_ID, LINK_ID, model_nm, instl_lc, instl_de)
        values(#{eld_id}, #{link_id}, #{model_nm}, #{instl_lc}, #{instl_de})
    </insert>

    <update id="eldUpdate">
        update Eld set instl_lc = #{instl_lc}, INSTL_DE = #{instl_de}
        where ELD_ID = #{eld_id} and LINK_ID = #{link_id}
    </update>


    <update id="eldUpdateSttus">
        update ELD_STTUS set LEAKAGE_ALARM = #{leakage_alarm}
        where ELD_ID = #{eld_id} and LINK_ID = #{link_id}
    </update>

    <delete id="eldDelete">
        delete from Eld where ELD_ID = #{eld_id} and LINK_ID = #{link_id}
    </delete>

    <select id="eldMonitor" resultType="com.highway.tunnelMonitoring.domain.power.eld.EldSttus">
        SELECT * FROM (
                          SELECT a.*, ROWNUM rnum
                          FROM (
                                   SELECT * FROM ELD_STTUS
                                   WHERE LINK_ID = #{link_id}
                                   ORDER BY
                                        <if test="sort_column == 'eld_id'">
                                            TO_NUMBER(REGEXP_SUBSTR(eld_id, '\d+'))
                                        </if>
                                        <if test="sort_column == 'leakage_alarm'">
                                            leakage_alarm
                                        </if>
                                        <if test="sort_direction == 'asc'">
                                            ASC
                                        </if>
                                        <if test="sort_direction == 'desc'">
                                            DESC
                                        </if>
                               ) a
                      )
        WHERE rnum BETWEEN #{offset} + 1 AND #{offset} + #{limit}
    </select>


    <insert id="eldCreateAlarmHistory">
        insert into ELD_ALARM_HISTORY(ELD_ID, LINK_ID, ALARM_DATETIME, ALARM_TYPE)
        values(#{eld_id}, #{link_id}, #{alarm_datetime}, #{alarm_type})
    </insert>

    <update id="eldUpdateAlarmHistory">
        update ELD_ALARM_HISTORY set RELEASE_DATETIME = #{release_datetime}
        where ELD_ID = #{eld_id} and LINK_ID = #{link_id} and ALARM_DATETIME = #{alarm_datetime} and ALARM_TYPE = #{alarm_type}
    </update>

    <select id="eldAlarmHistory" resultType="com.highway.tunnelMonitoring.domain.power.eld.EldAlarmHistroy">
        SELECT * FROM (
                          SELECT a.*, ROWNUM rnum
                          FROM (
                                   SELECT * FROM ELD_ALARM_HISTORY
                                   WHERE LINK_ID = #{link_id}
                                   AND ALARM_DATETIME >= #{startDate}
                                   AND ALARM_DATETIME &lt;= #{endDate}
                                   ORDER BY
                                        <if test="sort_column == 'eld_id'">
                                            TO_NUMBER(REGEXP_SUBSTR(eld_id, '\d+'))
                                        </if>
                                        <if test="sort_column == 'alarm_datetime'">
                                            alarm_datetime
                                        </if>
                                        <if test="sort_column == 'alarm_type'">
                                            alarm_type
                                        </if>
                                        <if test="sort_column == 'release_datetime'">
                                            release_datetime
                                        </if>
                                        <if test="sort_direction == 'asc'">
                                            ASC
                                        </if>
                                        <if test="sort_direction == 'desc'">
                                            DESC
                                        </if>
                               ) a
                      )
        WHERE rnum BETWEEN #{offset} + 1 AND #{offset} + #{limit}
    </select>




</mapper>